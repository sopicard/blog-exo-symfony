{% extends "base.html.twig" %}

{% block main %}
        {% for category in categories  %}
            <article>
                <h2 style="color: {{ category.color }}">{{ category.title }}</h2>
                <a href="{{ path("category",{"id":category.id}) }}">Clique sur le fruit de ton choix</a>
            </article>
        {% endfor %}

{% endblock %}

