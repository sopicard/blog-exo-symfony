{% extends "base.html.twig" %}

{% block main %}
    <section class="insert">
        <a class="btn-insert" href={{ path("admin_insert_category") }}>Ajouter une catégorie</a>
    </section>
    {% for category in categories  %}
    <section>
        <article>
            <h2 style="color: {{ category.color }}">{{ category.title }}</h2>
            <a href="{{ path("admin_category",{"id":category.id}) }}">Clique ici pour choisir ce fruit</a>
            <a href="{{ path("admin_delete_category",{"id":category.id}) }}">Supprimez ici votre fruit</a>
            <a href={{ path("admin_update_category", {"id": category.id}) }}> Modifiez ici</a>
        </article>
    </section>
    {% endfor %}
{% endblock %}

